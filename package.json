{
  "name": "rackmap",
  "version": "1.0.0",
  "private": true,
  "description": "Armenian LAN rack and patch panel manager",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "check": "node --check app.js && node --check domain.js && node --check server.js && node --check storage.js && node --check rack3d.js && node --check cloud-store.js && node --check cloud-auth.js && node --check local-repository.js && node --check api/index.js && node --check pwa.js && node --check sw.js && node --check scripts/cloud.js && node --check pin-auth.js && node --check env-config.js && node --check personal-store.js && node --check i18n.js && node --check locales.js && node --check scripts/setup-cloud-pins.js && node --check account-store.js && node --check drive-store.js",
    "build": "node scripts/build.js",
    "cloud:migrate": "node --env-file-if-exists=.env.local scripts/cloud.js migrate",
    "cloud:init": "node --env-file-if-exists=.env.local scripts/cloud.js init",
    "cloud:import": "node --env-file-if-exists=.env.local scripts/cloud.js import-sqlite",
    "cloud:export": "node --env-file-if-exists=.env.local scripts/cloud.js export",
    "cloud:restore": "node --env-file-if-exists=.env.local scripts/cloud.js restore",
    "pin:setup": "node scripts/setup-pin.js",
    "pin:cloud": "node --env-file-if-exists=.env.local scripts/setup-cloud-pins.js",
    "test:browser": "node scripts/audit-ui.js"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "pdfkit": "^0.17.2",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "acorn": "^8.18.0",
    "axe-core": "^4.13.0",
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^26.1.0",
    "playwright": "^1.63.0"
  },
  "overrides": {
    "exceljs": {
      "uuid": "^11.1.1"
    }
  }
}
